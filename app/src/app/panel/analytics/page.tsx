import { SectionStub } from "@/components/SectionStub";

export default function AnalyticsPage() {
  return (
    <SectionStub description="Метрики всех каналов, история для трендов и AI-разбор с рекомендациями. Здесь будут графики динамики, топ публикаций и подсветка узких мест." />
  );
}
