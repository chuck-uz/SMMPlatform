import { SectionStub } from "@/components/SectionStub";

export default function ConnectionsPage() {
  return (
    <SectionStub
      title="Подключения"
      description="Единое окно, где подключаются каналы: Instagram — через официальный вход Meta, Claude — вводом ключа. Здесь будет виден статус подключений и срок действия доступа."
    />
  );
}
