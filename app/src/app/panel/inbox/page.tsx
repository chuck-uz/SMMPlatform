import { SectionStub } from "@/components/SectionStub";

export default function InboxPage() {
  return (
    <SectionStub description="Все диалоги и заявки в одном окне. Здесь будут карточки заявок со статусами (новая / в работе / закрыта) и источником (директ / комментарий / сайт)." />
  );
}
